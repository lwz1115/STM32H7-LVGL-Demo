/**
 * @file performance_config.h
 * @brief LVGL性能优化配置
 */

#ifndef PERFORMANCE_CONFIG_H
#define PERFORMANCE_CONFIG_H

#ifdef __cplusplus
extern "C" {
#endif

/* ============================================
 * 1. 触摸响应优化
 * ============================================ */

/* 触摸采样率（Hz） - 越高越灵敏，但CPU占用越高 */
#define TOUCH_SAMPLE_RATE_HZ        50  /* 推荐：30-60Hz */

/* 触摸去抖动时间（ms） - 越小越灵敏 */
#define TOUCH_DEBOUNCE_MS           10  /* 推荐：10-20ms */

/* 长按触发时间（ms） */
#define TOUCH_LONG_PRESS_MS         400  /* 推荐：400-600ms */

/* ============================================
 * 2. 动画性能优化
 * ============================================ */

/* 动画帧率（FPS） */
#define ANIM_FPS                    30  /* 推荐：25-30FPS，平衡流畅度和性能 */

/* 动画时间（ms） */
#define ANIM_TIME_FAST              150  /* 快速动画 */
#define ANIM_TIME_NORMAL            250  /* 普通动画 */
#define ANIM_TIME_SLOW              400  /* 慢速动画 */

/* ============================================
 * 3. 渲染性能优化
 * ============================================ */

/* 刷新率（Hz） - LVGL任务调用频率 */
#define LVGL_TASK_HANDLER_PERIOD_MS  5   /* 5ms = 200Hz，推荐：5-10ms */

/* 脏区域优化 - 只重绘变化的区域 */
#define USE_DIRTY_RECT_OPTIMIZATION  1   /* 1:启用, 0:禁用 */

/* 双缓冲 - 减少闪烁但占用更多RAM */
#define USE_DOUBLE_BUFFER            0   /* 1:启用, 0:禁用（节省RAM） */

/* ============================================
 * 4. 内存优化
 * ============================================ */

/* LVGL内存池大小（字节） */
#define LVGL_MEM_SIZE               (64 * 1024)  /* 64KB，根据实际调整 */

/* 对象缓存数量 - 减少内存分配次数 */
#define OBJ_CACHE_SIZE              16   /* 推荐：8-32 */

/* 图片缓存大小（字节） */
#define IMG_CACHE_SIZE              (32 * 1024)  /* 32KB */

/* ============================================
 * 5. 字体优化
 * ============================================ */

/* 字体缓存 - 加速字体渲染 */
#define USE_FONT_CACHE              1    /* 1:启用, 0:禁用 */

/* 字体子像素渲染 - 更清晰但更慢 */
#define USE_FONT_SUBPX              0    /* 1:启用, 0:禁用（节省性能） */

/* ============================================
 * 6. 阴影和效果优化
 * ============================================ */

/* 阴影质量 */
#define SHADOW_QUALITY_LOW          0    /* 低质量，快速 */
#define SHADOW_QUALITY_MEDIUM       1    /* 中等质量 */
#define SHADOW_QUALITY_HIGH         2    /* 高质量，慢速 */

#define SHADOW_QUALITY              SHADOW_QUALITY_MEDIUM

/* 抗锯齿 */
#define USE_ANTI_ALIASING           1    /* 1:启用, 0:禁用（节省性能） */

/* ============================================
 * 7. 调试和监控
 * ============================================ */

/* 性能监控 - 显示FPS和CPU使用率 */
#define ENABLE_PERF_MONITOR         0    /* 1:启用, 0:禁用 */

/* 内存监控 - 显示内存使用情况 */
#define ENABLE_MEM_MONITOR          0    /* 1:启用, 0:禁用 */

/* ============================================
 * 8. 优化建议
 * ============================================ */

/*
 * 性能优化优先级：
 * 1. 减少不必要的重绘（使用脏区域）
 * 2. 降低动画复杂度
 * 3. 优化触摸采样率
 * 4. 减少阴影和特效
 * 5. 使用硬件加速（DMA2D）
 * 
 * 内存优化优先级：
 * 1. 禁用双缓冲
 * 2. 减少字体数量和大小
 * 3. 优化图片格式（使用压缩）
 * 4. 及时销毁不用的对象
 * 5. 减少对象缓存
 */

#ifdef __cplusplus
} /*extern "C"*/
#endif

#endif /*PERFORMANCE_CONFIG_H*/
